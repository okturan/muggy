## 1. Test harness and reference oracle

- [x] 1.1 Add `"test": "node --test test/"` to package.json with a trivial passing `test/smoke.test.js`; verify `npm test` exits 0
- [x] 1.2 Create `tools/wbgt-oracle/`. Add the Argonne `wbgt.c.original` with its notice intact, a `main.c` CSV driver, and a logged patch that accepts external `fdir` and `avg`. Verify `cc -O2 -lm` builds it and it reproduces the original program's output for the sample input in its header
  - Done as `oracle.c`, which links the untouched original (renaming only its demo `main` at compile time) rather than patching it. The original has no sample input in its header, so `verify.sh` runs the untouched demo program on `sample-input.txt` (Tirana, 13 Sep). It checks the driver matches on the 2 m and 10 m paths, and that the restated fdir path is bit-identical to `calc_wbgt()`.
- [x] 1.3 Write `tools/wbgt-oracle/make-fixtures.sh` to generate a ~20k-point grid (T 10–48 °C, RH 5–100 %, wind 0–15 m/s, irradiance 0–1100 W/m², fdir 0–0.9, solar elevations from below the horizon to overhead, several latitudes and dates). It writes `test/fixtures/wbgt-grid.json` with Tg, Tnwb and WBGT. Verify the file is written and committed
  - 17,621 cases, all converged. Written to `test/fixtures/wbgt-grid.json` and ready to commit; not committed yet, since commits wait for an explicit request.
- [x] 1.4 Add a Node-import purity test that imports every `public/lib/*.js` without `window`/`document`; verify it fails on a deliberately DOM-touching stub, then remove the stub

## 2. Heat-load engine

- [x] 2.1 Port Liljegren to `public/lib/wbgt.js` (solar position, Tglobe, Twb, transfer coefficients, esat, emis_atm, viscosity, conductivity, diffusivity, evap, stability class, 2 m wind) with the Argonne notice and modification log; verify every grid fixture point matches within 0.1 °C
  - Worst difference 0.0192 °C (Tg) across the grid, with 0 convergence disagreements.
- [x] 2.2 Add optional globe diameter, surface albedo, supplied zenith cosine and supplied direct fraction to the port, defaulting to the reference values, and log the change in the header; verify the oracle test passes unchanged and unit tests exercise each parameter
  - Oracle still matches (0.0192 °C) at defaults. The parameters are exercised by the load tests (globe 0.15, minimum wind override, supplied cosine via sun.js) and by the calibration grid (albedo).
- [x] 2.3 Implement MOE's psychrometric wet bulb in `public/lib/psychro.js`; verify agreement with the WMO psychrometer equation within 0.4 °C over 10–45 °C, 5–100 % RH and 850–1013 hPa, plus saturation and Ta = Td edge cases
  - Worst difference from the WMO psychrometer equation is 0.364 °C (45 °C, 5 % RH). Stull (2011) was dropped as a reference because its own error reaches about 1 °C.
- [x] 2.4 Implement the sunlit-part averaged zenith cosine in `public/lib/sun.js` (numerical quadrature of the port's solar position; sunlit fraction; dark fraction contributes shade); verify hourly evaluation is within 0.7 °C and 15-minute evaluation within 0.2 °C of minute-resolved means over the synthetic grid (6 latitudes × 4 dates × 3 air types)
  - Worst differences from minute-resolved means: 0.446 °C hourly and 0.192 °C for 15 minutes, across 6 latitudes × 4 dates × 3 air types. Checked against 1-second brute force at sunrise and sunset.
- [x] 2.5 Rewrite `public/lib/load.js` for the MOE definition (psychrometric Tw, 150 mm globe, calibration parameters from `calibration.js`, data-supplied direct fraction with estimate fallback, shared 2 m wind, night equality, missing-input rules, memoisation); verify a unit test per rule
  - Found and fixed along the way: the reference 50-iteration globe cap fails for a 150 mm globe in calm, strong sun. The engine now uses up to 2000 iterations, and a convergence test covers >10k cases so the engine can never quietly fall back to shade.
- [x] 2.6 Replace the smoothness and Ono tests with the heat-load spec's sanity requirements (Ono–Tonouchi ≥ 95 % within 2 °C; monotonic within 0.1 °C); verify both pass
  - With the shipped calibration: Ono–Tonouchi 100 % within 2 °C (mean −0.56 °C); largest monotonic decrease 0.032 °C over 18,900 checks.
- [x] 2.7 Implement the 150 mm globe with ISO 7726 convection in `public/lib/globe.js` (Argonne notice and modification note) and use it in `load.js`; verify the ISO coefficient tests, that it is stronger than the forced-only sphere correlation for a 150 mm globe, and that the convergence, load and minute-resolved tests still pass
  - 46/46 engine tests pass: Ono–Tonouchi 100 % within 2 °C (was 90.7 %), largest monotonic decrease 0.031 °C, minute-resolved worst 0.390 °C hourly and 0.166 °C for 15 minutes.

## 3. Validation against measured WBGT

- [x] 3.1 Write `tools/wbgt-validation/fetch.mjs`: download the MOE station master, MOE measured hours for the 49 stations (May–September from 2022, clipped to measured periods) and Open-Meteo historical forecast inputs for the same stations and seasons. Make it resumable and throttled, with raw files in git-ignored `data/`. Verify the station count is 49 and that every station/season has both files or a logged reason
  - 49 measured stations; 125 station-seasons, each with both an MOE file and an inputs file; 0 failed requests. Nerima and Hachioji stopped measuring in 2012, so they fall before the 2022 input window and have no seasons.
- [x] 3.2 Build `tools/wbgt-validation/dataset.mjs`: align JST observation hours with inputs (two radiation timing variants), keep only measured, quality-flagged hours with complete inputs, and assign stations to train/held-out deterministically, stratified by record length. Verify row counts per station and split are printed and that no station is in both sets
  - 434,405 measured hours (class 1, quality flag 4, complete inputs) from 47 stations: 27 training (237,758 h) and 20 held-out (196,647 h). No station is in both. Held-out long-record stations: Sapporo, Sendai, Niigata, Fukuoka, Kagoshima.
- [x] 3.3 Build `tools/wbgt-validation/evaluate.mjs`: metrics for any engine configuration (MAE, bias by wind band, irradiance band, hour, level; level agreement; severe under-warning), plus baselines for ISO Liljegren and Ono–Tonouchi. Verify it runs on the training set and prints the baseline table
  - Training-set baselines at reference parameters: MAE 1.07 °C, bias +0.33 °C, and +2.67 °C bias at ≥700 W/m². ISO Liljegren has MAE 1.21 °C. Ono–Tonouchi on forecast inputs has MAE 0.77 °C, bias +0.05 °C.
- [x] 3.4 Build `tools/wbgt-validation/calibrate.mjs`: grid over albedo × minimum 2 m wind × timing variant on training stations only, using the fixed decision rule. Verify by code inspection and a test that no held-out measured value is read
  - 30 configurations on 27 training stations. The fixed rule chose centred timing, albedo 0.25 and minimum wind 0.13 (17 candidates within 0.1 °C of the best MAE of 0.818; this one had the lowest severe miss). The no-leak test passes.
- [x] 3.5 Run the held-out evaluation of the chosen configuration. Write `results.json` and `RESULTS.md` and generate `public/lib/calibration.js` from them; verify the heat-load accuracy thresholds, or stop and report the missed metric with residual analysis
  - Run 3 (leave-one-station-out, ISO 7726 globe, revised rule): PASSES. Out-of-fold over 47 stations and 434,405 hours: MAE 0.79 °C, bias −0.16 °C, band biases −0.38…+0.56 °C, 99.8 % within one level, severe under-warning 0.13 % of 75,020 hours. All 47 folds chose albedo 0.20 and minimum wind 0.13. For comparison, Ono–Tonouchi on the same inputs has MAE 0.77 °C. Published in `RESULTS.md`, `results.json`, `history.json` and `calibration.js`.
  - Run 1 (2026-09-13, commit d8706bf): FAILED one threshold. Held-out MAE 0.88 °C, bias +0.07 °C, within one level 99.7 %, severe under-warning 0.1 % — but bias at ≥700 W/m² is +1.47 °C (limit ±1.0). Night bias −0.41 °C; midday +0.9 °C. Ono–Tonouchi on the same inputs has MAE 0.78 °C. Residual analysis on training stations (design D13.7) traced the strong-sun excess to the globe term (Tg +6.1 °C at ≥700 W/m², +9.7 °C below 1 m/s). Wet bulb and air temperature contribute only forecast-input error. Response: ISO 7726 globe convection (design D3.7, task 2.7) and albedo grid widened to 0.10. Run 2 will reuse the same held-out stations; that is recorded in `history.json` and `RESULTS.md`.
  - Run 2 (ISO 7726 convection): FAILED again on strong-sun bias, +1.11 °C. MAE 0.85 °C, bias 0.00 °C, within one level 99.8 %, severe 0.1 %. The old rule picked albedo 0.45, which already broke the band limit on the training stations. Response: the rule now requires every accuracy threshold on the choosing stations, and the protocol moves to leave-one-station-out cross-validation (task 3.7).
- [x] 3.6 Add `test/validation-results.test.js` asserting the published held-out metrics meet the heat-load thresholds and that `calibration.js` matches `results.json`; verify it passes
- [x] 3.7 Build `tools/wbgt-validation/crossval.mjs` (leave-one-station-out over all stations; mergeable per-station metrics; revised decision rule shared with `calibrate.mjs`), and make `validate.mjs` publish the out-of-fold metrics, the fold choices, the final all-station parameters and the run history. Verify the out-of-fold metrics meet the heat-load thresholds, or stop and report

## 4. Levels, texture and interpolation modules

- [x] 4.1 Implement `public/lib/levels.js` (round half-up, None/Easy/Noticeable/Tiring/Hard/Dangerous thresholds, alert marks at 33 and 35); verify the boundary table from the heat-load spec
- [x] 4.2 Implement hysteresis (0.3 °C around x.5, session map keyed by place and shade/sun, reset on place change); verify the 20.4/20.6/20.5/20.7 → Easy and 20.8 → Noticeable scenarios
- [x] 4.3 Implement `public/lib/texture.js` (bands 12.8/15.6/18.3/21.1/23.9, ranks); verify the band-edge scenario
- [x] 4.4 Move the shared minute interpolation into `public/lib/interp.js`, including the new radiation and pressure fields; verify the client and worker produce identical current readings for a fixture at five different minutes
  - `test/interp-shared.test.js`: the worker's `previewVerdict` reading equals the client's `interpolateNow` at 10:31, 10:37, 10:44, 10:52 and 11:06, including radiation, wind and pressure.

## 5. Verdict composition and copy gating

- [x] 5.1 Create `public/lib/lexicon.js` with the phrase classes (reassurance, pace, rest-and-water, avoid-exertion, stop-and-cool, indoor, sun-or-shade, activity-advice, load-level words); verify unit tests that each class matches its examples and none of a negative list
- [x] 5.2 Write `public/lib/copy.js`: headlines for texture × level, split templates, texture sentences (day/night, no activity advice, hydration only for dry), load sentences per level (day/night), qualifiers. Verify the texture-lexicon test passes
- [x] 5.3 Implement `compose()` in `public/lib/verdict.js` (texture-led at None/Easy with the mild qualifier for muggy and wetter, load-led with texture qualifier at Noticeable–Hard, danger without texture, split headline when sun > shade, night rules, three-sentence blurb order, texture-only when load is unavailable); verify unit tests for each composition scenario in the comfort-verdict spec
- [x] 5.4 Build the reachability sweep: run the engine over a dense input grid, collect reachable texture × shade × sun × day/night × sunKnown tuples, and snapshot them to `test/fixtures/reachable.json`. Verify every reachable tuple composes without missing copy
  - `tools/reachability.mjs`: 158,800 engine evaluations (4 moments, T −5…50 °C, every dew point, 4 winds, 5 irradiances, plus radiation-unknown and load-unavailable variants) give 186 reachable combinations.
- [x] 5.5 Add the exhaustive gating test (reassurance needs both levels ≤ Easy; severity tiers; indoor only at night or ≥ Hard; no sun/shade words at night; sun-only severity labelled); verify it passes, and fails when a deliberately bad string is injected
  - All 186 compose and pass every rule. The test fails on injected pace advice at Easy and on activity advice injected into texture copy.

## 6. Explainer, relief and normals logic

- [x] 6.1 Implement exact 3-factor Shapley attribution in `public/lib/explain.js` against the reference state, plus word buckets; verify sums within 0.05 °C across the grid and the 38 °C strong-wind case is not called helpful
  - Shares sum exactly (worst 0, by construction) over 216 sun/shade cases; the breeze is only called helpful when its share is ≤ −0.5.
- [x] 6.2 Implement peak and trend sentence selection (suppressed at Easy unless a higher level comes later today, "as heavy as today gets" only at ≥ Noticeable, time tags on every non-now level); verify unit tests for the quiet-morning and suppressed-drama scenarios and run the gating scan over the outputs
- [x] 6.3 Implement `public/lib/relief.js` (composite rank of worst level then texture, texture-only when current ≤ Easy, no opening 00:00–05:59, run extension and bottom, cause wording for sun-down vs drier air, no-relief variants); verify unit tests for every relief-window scenario and the gating scan over its sentences
  - Along the way, fixed a contradiction in the relief spec: one scenario reported relief from 01:00, but 00:00–05:59 hours cannot open a window. The scenario now uses 23:00.
- [x] 6.4 Implement `public/lib/normals.js` (percentile from the hour ladder, dayparts, verdict thresholds, record wording, bar shares); verify unit tests for heading/body agreement, morning wording and record-level stickiness

## 7. Worker

- [x] 7.1 Add `direct_radiation`, `diffuse_radiation` and `surface_pressure` to current, minutely_15 and hourly upstream params; bump the KV forecast prefix to `f2:` in the fetch path and the cron pre-warmer. Verify with `wrangler dev` that `/api/forecast` returns the new fields and that KV writes use `f2:`
  - Verified with `wrangler dev` (local HTTPS). Current, 15-minute and hourly data include direct radiation, diffuse radiation and surface pressure; the local KV key is `f2:41.33:19.82`.
- [x] 7.2 Rebuild `/api/normals` as v4 with per-local-hour ±2 h pooled ladders, band shares and sample counts (drop `days`), 400/503 rules and the `v4:` key. Verify with `wrangler dev` for Tirana, Reykjavík and Singapore: 24 entries, each with ≥ 200 samples, and a 400 without coordinates
  - Verified: 400 without coordinates. Tirana, Reykjavík and Singapore return 24 hour entries with 101-point ladders and 750 samples each, cached under `v4:` keys.
- [x] 7.3 Replace the worker's `bandOf`/`OG_LINE`/`interpNow` link-preview logic with imports from `public/lib` (interp, texture, load, levels, verdict). Title uses the composed headline, description says "air: <band>", image stays per band. Verify `curl -s localhost:8787/tirana | grep og:title` shows the same headline as the app at that minute
  - Verified locally: the `/tirana` og:title ("Easy in the shade, noticeable in the sun in Tirana right now") matches `previewVerdict` computed independently from the same forecast at the same minute. The worker imports interp, texture, load, levels and verdict from `public/lib`; `bandOf`, `OG_LINE` and `interpNow` are gone.

## 8. Client screen

- [ ] 8.1 Switch `index.html` to `<script type="module" src="/app.js">`, import the lib modules, and add all `public/lib/*.js` to the service worker CORE with cache `muggy-v2`. Verify the app loads in Chrome and Safari with no console errors and works offline after one visit
  - Chrome verified: no console errors, and the page renders offline after one visit (e2e "offline" check). **Safari not yet verified**, because it cannot be automated from this environment; it needs a manual check before this box is ticked.
- [x] 8.2 Render the composed headline and blurb, the title and share text from `compose()`; rename the Comfort tile to Air. Verify the Puppeteer `tirana-2026-09-13-1030` fixture shows no contradictory reassurance or advice anywhere in the DOM
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`. The tirana-2026-09-13-1030 fixture now reads "Muggy and warm — Shirts start sticking, and sweat is slow to dry. A hill or a fast walk will remind you, so keep the pace easy." with no contradictory reassurance anywhere.
- [x] 8.3 Rebuild the strain card as "Out in it" (factor bars with word buckets, peak/trend sentence, small print with rounded shade and sun WBGT linking to /about#wbgt, hidden at None or unavailable load). Verify the `split-noon` and `muggy-mild-dawn` fixtures
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`: split-noon shows shade and sun WBGT and three factors; muggy-mild-dawn shows the breakdown.
- [x] 8.4 Add the "Why this verdict?" dialog (opened from the headline and the card's Why control; the six sections; focus in, trap, Escape and backdrop close, focus return). Verify the Puppeteer keyboard test and that the sheet's numbers and levels match the screen
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`: keyboard open puts focus on Close, Escape closes and returns focus, and the sheet's numbers match the small print.
- [x] 8.5 Wire hysteresis into the minute tick and the place change. Verify a Puppeteer test that advances a frozen clock across a boundary fixture and asserts no flicker
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`: the boundary fixture's raw rounded level flips 4 times in the hour, while the headline stays constant over 60 minute ticks.
- [x] 8.6 Rewrite the normals card on the time-of-day basis (single statistic, daypart wording, bar on the same set, hidden without hour ladders). Verify the Puppeteer fixture for heading/body agreement
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`: every fixture's normals card states one statistic and its complement ("stickier than 78% of mornings" / "Only 22% …").
- [x] 8.7 Rewrite the relief card on the composite ranking with cause wording and tint by target band. Verify the `sunset-relief` and `night-oppressive` fixtures
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`: sunset-relief and night-oppressive cards match the shared relief ("Tomorrow, from 06:00").
- [x] 8.8 Relabel the hours strip and week sub-headings as stickiness-only. Verify the lexicon scan finds no load words in either
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`: no load-level words in either sub-heading on any fixture.
- [x] 8.9 Handle the load-unavailable and missing-radiation paths in the UI. Verify the `missing-radiation` fixture (no split, no sun words) and a no-temperature fixture (texture-only, card hidden)
  - Verified by `npm run test:e2e` (98/98 checks) against `wrangler dev`: missing-radiation shows no split and no sun small print; no-temperature is texture-only with "Out in it" and "Why" hidden.

## 9. About page, README and credits

- [x] 9.1 Rewrite `public/about.html`: two readings with the band × level grid and the "muggy but mild" example, the level ladder with JSBM/MOE sources, plain-language WBGT and shade/sun, grid-cell and personal-factor caveats, and a technical section that includes the MOE-definition choice and a summary of the held-out validation results. Add a note on the earlier humidex versions and credits (Open-Meteo, WeatherSpark, MOE data, verbatim Argonne acknowledgment). Verify an automated test that the ladder ranges equal `levels.js`, the published metrics match `results.json`, and the acknowledgment string is present
  - `test/about.test.js` passes: the ladder equals `levels.js`, the published metrics equal `results.json` and `calibration.js`, and the Argonne acknowledgment is verbatim. The page also covers the band × level grid from the reachability sweep, plain-language WBGT, shade/sun, caveats, the technical section and a note on the earlier index.
- [x] 9.2 Build the explorer on About (temperature, texture, sun, wind controls; headline, levels and factor bars from the shared modules; aria-live result). Verify a Puppeteer test that explorer inputs equivalent to a fixture reproduce the main screen's headline, plus the keyboard-only tab test
  - `test/e2e/about.e2e.mjs` 7/7: the explorer's headline and blurb equal `explore()` from the shared modules for four input sets; every control is Tab-reachable; arrow keys update an aria-live result; no console errors. (The check compares against the engine directly rather than a main-screen fixture, since the explorer's idealised sun and wind don't map onto a forecast fixture; both paths share `compose` and `wbgtInterval`.)
- [x] 9.3 Update the About page's FAQ JSON-LD to match the visible text; verify a test that parses the JSON-LD and finds no current-method humidex mention and matching ranges
  - Verified in `test/about.test.js`: JSON-LD parses, no answer mentions humidex, and band edges, level ranges and the accuracy figure match.
- [x] 9.4 Update README (hierarchy, WBGT engine and definition, validation summary, levels, normals basis, acknowledgment); verify by review that no section describes humidex as the current engine
  - Reviewed: README describes the two readings, the MOE-definition WBGT engine, the validation table (out-of-fold 0.79 °C vs Ono–Tonouchi 0.77 °C), levels, normals v4 and the acknowledgment. Humidex appears only as the earlier, rejected design.

## 10. End-to-end verification and release

- [x] 10.1 Add the Puppeteer performance test (4× CPU throttle, 7-day fixture, load calculation measured under 50 ms); verify it passes locally
  - `test/e2e/perf.e2e.mjs`: 168-hour fixture at 4× CPU throttle; the cold first render's heat-load span (reading, series and breakdown, plus hours/week DOM) took 33.5 ms, under the 50 ms budget.
- [x] 10.2 Run the full suite (`npm test` plus the Puppeteer fixtures against `wrangler dev`); verify all pass and capture screenshots of each named fixture for review
  - `npm test` 125/125; `npm run test:e2e`: main screen 98/98, About 7/7, performance ok. Screenshots of every named fixture are in `test/e2e/screenshots/` (git-ignored).
  - Final check (2026-09-14) found and fixed four things the suites had missed: `.block { display: flex }` overrode the `hidden` attribute, so hidden cards rendered as empty boxes (now `[hidden] { display: none !important }`); the week view printed "-Infinity°" when temperatures were missing; the reachable None→Easy split composed with a silently dropped sentence (splits now require a sun level of Noticeable or above, and a missing string throws); and the small print could print a WBGT outside a level held by hysteresis (now clamped, with the exact value and an "on the line" note in the Why sheet). Also "Easing to none" in the relief card, a duplicated attribution call, and `package.json` now declares `"type": "module"`. The browser suite gained checks for hidden cards, stray Infinity/NaN/undefined and double spaces (125/125).
  - Second-reviewer pass (`/code-review high`, 2026-09-14) reported ten findings, all fixed: the 5-minute refresh tick lacked the `loadSeq` guard; relief and peak compared the held current level with raw hourly levels (hours are now levelled relative to the held level via `stepLevel`); texture-only relief could offer heavier hours; validation timing ("centred") differed from the app's hourly alignment (both figures now published); the level word "none" leaked into the Why sheet and relief card; the cold-air dry reference saturated below 10 °C; relief windows bridged missing hours; the trend spanned ~2 h and flickered (now the difference of the two latest hourly means); day copy showed for up to 15 minutes after sunset (day/night is now the sun at the current minute); "Easing to easy" when only the air dried. Also: unknown wind no longer breaks the factor sum, the OG description tolerates missing values, `/about#humidex` still resolves, and the module graph is preloaded.
- [x] 10.3 Deploy with `wrangler deploy`; verify production `/api/forecast` has the new fields, `/api/normals` returns v4, `muggy.fyi/tirana` link previews use the composed headline, and the named-city screens render with no console errors
  - Deployed 2026-09-14 (version 8316ff74) with `env -u CLOUDFLARE_API_TOKEN npx wrangler deploy`; the shell's API token lacks Workers permissions, the OAuth login works. Production: forecast has the new fields, normals v4 (400 without coordinates), `/lib/*.js` served as text/javascript with the Argonne notice, SW muggy-v2, About live. `/tirana` og:title equals the shared verdict. Browser suites against production: 125/125, About 7/7, perf 32.7 ms. Live renders (`test/e2e/live.e2e.mjs`) for Tirana, Singapore, Reykjavík, Dubai, Sydney: clean.
- [x] 10.4 Audit every text in every reachable scenario and fix what doesn't read right
  - 2026-09-22: every text branch enumerated (186 reachable verdict combinations, trend, cause, relief and normals templates), plus 55 real cities stepped through 22 hours in the real app (880 snapshots, Why sheet included).
  - Fixed: cool damp air called "Crisp and dry" or "Perfect air" (41% of snapshots) now reads "Cool and damp" or "Cold and damp"; hot dry air no longer "fresh"; "Still air makes it makes it worse"; relief "as good as it gets" on already fine air (card now hidden) and "Right now" for no relief; one-hour blips, open-ended windows, sunset credited at 06:00, and windows cut short by a wobble in the air; trend lines that contradicted each other; the normals card repeating its number flipped, calling dry winter air "stickier", and "Humid air is rare here" in Manaus; equal shade and sun boxes; a duplicate "Why this verdict?" control; "tonight" twice in one blurb; "shady" at night and "black globe" jargon in the Why sheet.
  - `npm test` 153/153; main screen 146/146; About 7/7. The performance budget fails at 56–115 ms on this machine for both this change and the previous commit, so it is an environment regression, not this change.
  - Follow-up (2026-09-22): the Air tile and the chip on the character name the air in words that agree with the headline, "damp" in fog and "not sticky" for comfortable air under real heat, while colour and timelines still follow the band. The normals card hides in fog. The dry and comfortable link banners now read "NOT STICKY": "CRISP AND DRY" had been printed under "Cool and damp" and "Tiring, dry heat" titles, and "PERFECT AIR" over Dubai at 40 °C. `npm test` 153/153; main screen 190/190 with two new fixtures, foggy-winter and hot-not-sticky; About 7/7.
